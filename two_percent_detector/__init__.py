"""2% Detector — multi-platform chat monitor (read-only).

Watches Twitch, Kick, and Rumble live-stream chats and raises a terminal alert whenever
a user sends the same (or substantially similar) message multiple times within a rolling
detection window.
"""
